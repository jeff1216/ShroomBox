[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fruitbox-core"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "gymnasium",
    "numpy",
]

[tool.hatch.build.targets.wheel]
packages = ["src/fruitbox_core"]
